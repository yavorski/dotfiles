#### random notes

```shell
# Default arch prompt (left here just for ref)
# PS1='[\u@\h \W]\$ ';
```


```shell
# If not running interactively, don't do anything

case $- in
  *i*) ;;
    *) return;;
esac
```
